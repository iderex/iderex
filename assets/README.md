# assets

Place the profile banner here as `banner.png` (the image shown in chat).

The README references it as `assets/banner.png`. Recommended: a wide image
(e.g. ~1500×500 px). GitHub also renders `.jpg`, `.webp`, and `.gif` — if you
use a different extension, update the `<img src=...>` in `../README.md`.
